apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-cache
  labels:
    app.kubernetes.io/part-of: vllm-disagg
spec:
  # storageClassName: set via STORAGE_CLASS in env.sh (deploy.sh generates PVC)
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
